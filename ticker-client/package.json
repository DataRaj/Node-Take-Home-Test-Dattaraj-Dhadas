{
  "name": "betacrew",
  "version": "1.0.0",
  "description": "This Node.js client communicates with the BetaCrew exchange server via TCP, requests stock ticker data, and generates a JSON file with no missing sequences.",
  "main": "src/index.ts",  
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.js",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "type": "commonjs",     
  "devDependencies": {
    "@types/express": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
